# 1stApp

A beautiful iOS app built with SwiftUI.

## Features

- ✨ Modern, gradient-based UI design
- 🔢 Interactive counter with increment/decrement buttons
- 👋 Personalized greeting with name input
- 🎨 Smooth animations and transitions
- 📱 Responsive design for iPhone

## Requirements

- Xcode 14.0 or later
- iOS 15.0 or later
- macOS (for development)

## Setup Instructions

1. **Open in Xcode:**
   - Open Xcode
   - Select "File" → "Open" and navigate to this project folder
   - Or create a new Xcode project and add these Swift files

2. **Create Xcode Project (if needed):**
   - Open Xcode
   - Create a new project (File → New → Project)
   - Choose "iOS" → "App"
   - Product Name: `1stApp`
   - Interface: SwiftUI
   - Language: Swift
   - Replace the generated files with the files in this directory

3. **Configure Project:**
   - Set your development team in the project settings
   - Choose a unique Bundle Identifier
   - Select your target device (iPhone simulator or physical device)

4. **Run the App:**
   - Select a simulator or connected iPhone
   - Press `Cmd + R` or click the Run button
   - The app will build and launch on your selected device

## Project Structure

```
1stApp/
├── 1stAppApp.swift      # Main app entry point
├── ContentView.swift     # Main UI view
└── Info.plist           # iOS app configuration
```

## Features Overview

- **Counter**: Tap the + and - buttons to increment or decrement the counter
- **Reset**: Tap "Reset" to set the counter back to zero
- **Name Input**: Enter your name in the text field for a personalized experience
- **Hello Button**: Tap "Say Hello" to see a greeting alert

Enjoy your first iOS app! 🎉